# Windows switcher
#
# Needs `wmctrl` installed and window class name (check with `wmctrl -l -x`)


# telegram
super + o ; t
    wmctrl -x -a telegram.64Gram || tg

# web-browser
super + o ; b
    wmctrl -x -a Navigator.firefox || firefox

# code editor
super + o ; c
    wmctrl -x -a code.Code

# git client
super + o ; g
    wmctrl -x -a sublime_merge.Sublime_merge

# obsidian
super + o ; o
    wmctrl -x -a obsidian.obsidian || obsidian

# postman
super + o ; p
    wmctrl -x -a postman.Postman || postman

# warpd
super + c
	warpd --hint

